# Backend Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# CORS Configuration
# Development: http://localhost:4200
# Production with IP: http://YOUR_DROPLET_IP
# Production with domain (HTTP): http://yourdomain.com
# Production with domain (HTTPS): https://yourdomain.com
CORS_ORIGIN=http://170.64.166.2:4200

# Rate Limiting (requests per time window)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000

# API Endpoint Rate Limits (requests per minute)
RATE_LIMIT_EVALUATE=30
RATE_LIMIT_RULES_WRITE=10
RATE_LIMIT_RULES_READ=60

# Production Configuration Notes:
# 1. Set NODE_ENV=production for production deployments
# 2. Update CORS_ORIGIN to match your droplet IP or domain
# 3. When using SSL (HTTPS), update CORS_ORIGIN to use https://
# 4. Adjust rate limits based on your expected traffic
